# M337 Plan：兼容模块分类防回归

## M337-A：分类事实收口

- [x] 将三类清单固定为不可变集合，并保留兼容聚合导出。
- [x] 增加分类 schema 版本和逐模块分类报告。
- [x] 加入公共模块存在性、文件类型和分类清单完整性检查。

## M337-B：shim 形状门禁

- [x] 使用 AST 检查 shim 只包含文档、导入、未来导入和字符串 `__all__`。
- [x] 为膨胀 shim、包含实现节点或没有转发导入的情况返回明确错误码。
- [x] 不把 `COMPAT_FACADES` 应用 shim 形状规则，保留其有限适配空间。

## M337-C：紧凑契约与文档

- [x] 增加 M337 架构契约测试，覆盖正常分类及异常注入。
- [x] 更新兼容矩阵、中文问题日志和交接文档，明确分类规则。
- [x] 更新热状态和文档索引，只保留 M337 恢复入口。

## M337-D：Docker 验证与交付

- [x] 执行受影响契约、compileall 和 architecture strict。
- [x] 核对 diff、密钥和运行产物未进入提交。
- [ ] 阶段完成后提交并推送版本，再按项目全局目标规划下一阶段。

## 风险与回滚

- 现有 shim 若包含历史兼容常量，只能先归入 facade 或拆到 canonical 模块；不通过放宽守卫掩盖。
- 若旧测试依赖 `set` 可变性，保留集合运算接口但使用不可变集合；必要时只修正测试夹具，不改变生产语义。
- 本阶段不删除任何旧导入路径，回滚只需恢复守卫与契约文档变更。
