# 阶段文档包

每个开发阶段使用一个独立目录 `Mxxx/`，只保留四份互相链接的文件：

1. `capability-map.md`：模块边界和依赖方向。
2. `spec.md`：公共契约、边界和验收标准。
3. `plan.md`：实现任务和验证顺序。
4. `handoff.md`：当前工作快照和下一步恢复入口。

`handoff.md` 是恢复时唯一默认读取的阶段文档。阶段完成后将其冻结；后续修复应创建新的
子阶段或在当前 Goal 中明确记录，不回写已完成阶段的历史总结。
